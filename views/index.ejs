<%- include('./layout/cabecera.ejs') %>

<div class="container">
  <br>
  <div id="theCarousel" class="carousel slide" data-bs-ride="carousel" data-bs-interval="3000">
      <!-- INDICADORAS, SOLO SON LAS IMAGENES DE INICIO NADA IMPORTANTE-->
      <ol class="carousel-indicators">
          <li data-bs-target="#theCarousel" data-bs-slide-to="0" class="active"></li>
          <li data-bs-target="#theCarousel" data-bs-slide-to="1"></li>
          <li data-bs-target="#theCarousel" data-bs-slide-to="2"></li>
      </ol>

      <div class="carousel-inner">
          <div class="carousel-item active">
              <img src="/images/selena.png" class="d-block w-100" alt="Ilustracion" style="height: 700px; object-fit: cover;"> 
              <div class="carousel-caption d-none d-md-block">
                  <h3>BELLEZA</h3>
              </div>
          </div>

          <div class="carousel-item">
              <img src="/images/cereal.png" class="d-block w-100" alt="Pintura" style="height: 700px; object-fit: cover;">
              <div class="carousel-caption d-none d-md-block">
                  <h3>DESAYUNO</h3>
              </div>
          </div>
          <div class="carousel-item">
              <img src="/images/colgate.png" class="d-block w-100" alt="Diseño" style="height: 700px; object-fit: cover;">
              <div class="carousel-caption d-none d-md-block">
                  <h3>SALUD DENTAL</h3>
              </div>
          </div>
      </div>

      <a class="carousel-control-prev" href="#theCarousel" role="button" data-bs-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Anterior</span>
      </a>
      <a class="carousel-control-next" href="#theCarousel" role="button" data-bs-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Siguiente</span>
      </a>
  </div>
</div>



<!-- RECOMENDACIONES BASADAS EN TI -->
<div class="container mt-5">
    <h3>RECOMENDACIONES BASADAS EN TI</h3>
    <div id="recomendacionCareCarousel" class="carousel slide" data-bs-ride="false">
        <!-- Indicadores -->
        <ol class="carousel-indicators">
            <% reglasApriori.forEach((regla, index) => { %>
                <li data-bs-target="#recomendacionCareCarousel" data-bs-slide-to="<%= index %>" class="<%= index === 0 ? 'active' : '' %>"></li>
            <% }) %>
        </ol>

        <div class="carousel-inner">
            <% reglasApriori.forEach((regla, index) => { %>
                <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                    <div class="row">
                        <div class="col-12 text-center">
                            <h5>Productos recomendados:</h5>
                            <p><%= regla.items.join(', ') %></p> <!-- Muestra los productos recomendados -->
                            <p><strong>Soporte:</strong> <%= regla.support %></p> <!-- Muestra el soporte de la regla -->
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>

        <!-- Controles del carrusel -->
        <a class="carousel-control-prev" href="#recomendacionCareCarousel" role="button" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Anterior</span>
        </a>
        <a class="carousel-control-next" href="#recomendacionCareCarousel" role="button" data-bs-slide="next" style="background-color: #007bff; color: #fff;">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Siguiente</span>
        </a>
    </div>
</div>


        <!-- Controles del carrusel -->
        <a class="carousel-control-prev" href="#recomendacionCareCarousel" role="button" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Anterior</span>
        </a>
        <a class="carousel-control-next" href="#recomendacionCareCarousel" role="button" data-bs-slide="next" style="background-color: #007bff; color: #fff;">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Siguiente</span>
        </a>
    </div>
</div>


<!-- CUIDADO PERSONAL -->
<div class="container mt-5">
    <h3>CUIDADO PERSONAL</h3>
    <div id="personalCareCarousel" class="carousel slide" data-bs-ride="false">
        <!-- Indicadores -->
        <ol class="carousel-indicators">
            <% productos.forEach((producto, index) => { %>
                <li data-bs-target="#personalCareCarousel" data-bs-slide-to="<%= index %>" class="<%= index === 0 ? 'active' : '' %>"></li>
            <% }) %>
        </ol>

        <div class="carousel-inner">
            <div class="carousel-item active">
                <div class="row">
                    <% productos.slice(0, 4).forEach((producto) => { %>
                        <div class="col text-center">
                            <div class="product-box">
                                <img src="<%= producto.imagen %>" class="img-fluid" alt="<%= producto.nombre %>" style="max-height: 200px; object-fit: contain;">
                                <h5><%= producto.nombre %></h5>
                                <h3>$<%= producto.precio %></h3>
                                <form action="" method="post">
                                    <input type="hidden" name="productoId" value="<%= producto.id_productos %>">
                                    <button type="submit" class="btn btn-primary mt-2 btn-custom">Seleccionar</button>
                                </form>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
            <% for (let i = 4; i < productos.length; i += 4) { %>
                <div class="carousel-item">
                    <div class="row">
                        <% productos.slice(i, i + 4).forEach((producto) => { %>
                            <div class="col text-center">
                                <div class="product-box">
                                    <img src="<%= producto.imagen %>" class="img-fluid" alt="<%= producto.nombre %>" style="max-height: 200px; object-fit: contain;">
                                    <h5><%= producto.nombre %></h5>
                                    <h3>$<%= producto.precio %></h3>
                                    <form action="" method="post">
                                        <input type="hidden" name="productoId" value="<%= producto.id_productos %>">
                                        <button type="submit" class="btn btn-primary mt-2  btn-custom">Seleccionar</button>
                                    </form>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </div>
            <% } %>
        </div>

        <a class="carousel-control-prev" href="#personalCareCarousel" role="button" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Anterior</span>
        </a>
        <a class="carousel-control-next" href="#personalCareCarousel" role="button" data-bs-slide="next" style="background-color: #007bff; color: #fff;">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Siguiente</span>
        </a>
    </div>
</div>



<!-- SOLO PRODUCTOS VIDEOJUEGOS-->
<div class="container mt-5">
    <h3>VIDEOJUEGOS</h3>
    <div id="videoGamesCarousel" class="carousel slide" data-bs-ride="false">
        <!-- Indicadores -->
        <ol class="carousel-indicators">
            <% productos2.forEach((producto, index) => { %>
                <li data-bs-target="#videoGamesCarousel" data-bs-slide-to="<%= index %>" class="<%= index === 0 ? 'active' : '' %>"></li>
            <% }) %>
        </ol>

        <div class="carousel-inner">
            <div class="carousel-item active">
                <div class="row">
                    <% productos2.slice(0, 4).forEach((producto) => { %>
                        <div class="col text-center">
                            <div class="product-box">
                                <img src="<%= producto.imagen %>" class="img-fluid" alt="<%= producto.nombre %>" style="max-height: 200px; object-fit: contain;">
                                <h5><%= producto.nombre %></h5>
                                <h3>$<%= producto.precio %></h3>
                                <form action="" method="post">
                                    <input type="hidden" name="productoId" value="<%= producto.id_productos %>">
                                    <button type="submit" class="btn btn-primary mt-2">Seleccionar</button>
                                </form>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
            <!-- PARA MOSTRAR LA INFO CORRESPODIENTE UTILIZAMOS UN FOR PARA RECORRER LOS PRODUCTOS CORRESPONDIENTES -->
            <% for (let i = 4; i < productos2.length; i += 4) { %> 
                <div class="carousel-item">
                    <div class="row">
                        <% productos2.slice(i, i + 4).forEach((producto) => { %>
                            <div class="col text-center">
                                <div class="product-box">
                                    <img src="<%= producto.imagen %>" class="img-fluid" alt="<%= producto.nombre %>" style="max-height: 200px; object-fit: contain;">
                                    <h5><%= producto.nombre %></h5>
                                    <h3>$<%= producto.precio %></h3>
                                    <!-- AQUI MANDAMOS EL PRODUCTO PARA VER LA INFO METODO POST -->
                                    <form action="" method="post"> 
                                        <input type="hidden" name="productoId" value="<%= producto.id_productos %>">
                                        <button type="submit" class="btn btn-primary mt-2">Seleccionar</button>
                                    </form>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </div>
                <!-- CERRAMOS POR ACA, PARA LLAMAR LOS DATOS SOLO ES CON EL ITERADOR.NOMBREDELATRIBUTO -->
            <% } %> 
        </div>

        <a class="carousel-control-prev" href="#videoGamesCarousel" role="button" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Anterior</span>
        </a>
        <a class="carousel-control-next" href="#videoGamesCarousel" role="button" data-bs-slide="next" style="background-color: #007bff; color: #fff;">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Siguiente</span>
        </a>
    </div>
</div>




<!-- VETERINARIA -->

<div class="container mt-5">
    <h3>VETERINARIA</h3>
    <div id="CroquetasCarousel" class="carousel slide" data-bs-ride="false">
        <!-- Indicadores -->
        <ol class="carousel-indicators">
            <% veterinaria.forEach((producto, index) => { %>
                <li data-bs-target="#CroquetasCarousel" data-bs-slide-to="<%= index %>" class="<%= index === 0 ? 'active' : '' %>"></li>
            <% }) %>
        </ol>

        <div class="carousel-inner">
            <div class="carousel-item active">
                <div class="row">
                    <% veterinaria.slice(0, 4).forEach((producto) => { %>
                        <div class="col text-center">
                            <div class="product-box">
                                <img src="<%= producto.imagen %>" class="img-fluid" alt="<%= producto.nombre %>" style="max-height: 200px; object-fit: contain;">
                                <h5><%= producto.nombre %></h5>
                                <h3>$<%= producto.precio %></h3>
                                <form action="" method="post">
                                    <input type="hidden" name="productoId" value="<%= producto.id_productos %>">
                                    <button type="submit" class="btn btn-primary mt-2">Seleccionar</button>
                                </form>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
            <!-- PARA MOSTRAR LA INFO CORRESPODIENTE UTILIZAMOS UN FOR PARA RECORRER LOS PRODUCTOS CORRESPONDIENTES -->
            <% for (let i = 4; i < veterinaria.length; i += 4) { %> 
                <div class="carousel-item">
                    <div class="row">
                        <% veterinaria.slice(i, i + 4).forEach((producto) => { %>
                            <div class="col text-center">
                                <div class="product-box">
                                    <img src="<%= producto.imagen %>" class="img-fluid" alt="<%= producto.nombre %>" style="max-height: 200px; object-fit: contain;">
                                    <h5><%= producto.nombre %></h5>
                                    <h3>$<%= producto.precio %></h3>
                                    <!-- AQUI MANDAMOS EL PRODUCTO PARA VER LA INFO METODO POST -->
                                    <form action="" method="post"> 
                                        <input type="hidden" name="productoId" value="<%= producto.id_productos %>">
                                        <button type="submit" class="btn btn-primary mt-2">Seleccionar</button>
                                    </form>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </div>
                <!-- CERRAMOS POR ACA, PARA LLAMAR LOS DATOS SOLO ES CON EL ITERADOR.NOMBREDELATRIBUTO -->
            <% } %> 
        </div>

        <a class="carousel-control-prev" href="#CroquetasCarousel" role="button" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Anterior</span>
        </a>
        <a class="carousel-control-next" href="#CroquetasCarousel" role="button" data-bs-slide="next" style="background-color: #007bff; color: #fff;">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Siguiente</span>
        </a>
    </div>
</div>




<!-- SALUD -->


<div class="container mt-5">
    <h3>SALUD</h3>
    <div id="saludCarousel" class="carousel slide" data-bs-ride="false">
        <!-- Indicadores -->
        <ol class="carousel-indicators">
            <% salud.forEach((producto, index) => { %>
                <li data-bs-target="#saludCarousel" data-bs-slide-to="<%= index %>" class="<%= index === 0 ? 'active' : '' %>"></li>
            <% }) %>
        </ol>

        <div class="carousel-inner">
            <div class="carousel-item active">
                <div class="row">
                    <% salud.slice(0, 4).forEach((producto) => { %>
                        <div class="col text-center">
                            <div class="product-box">
                                <img src="<%= producto.imagen %>" class="img-fluid" alt="<%= producto.nombre %>" style="max-height: 200px; object-fit: contain;">
                                <h5><%= producto.nombre %></h5>
                                <h3>$<%= producto.precio %></h3>
                                <form action="" method="post">
                                    <input type="hidden" name="productoId" value="<%= producto.id_productos %>">
                                    <button type="submit" class="btn btn-primary mt-2">Seleccionar</button>
                                </form>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
            <!-- PARA MOSTRAR LA INFO CORRESPODIENTE UTILIZAMOS UN FOR PARA RECORRER LOS PRODUCTOS CORRESPONDIENTES -->
            <% for (let i = 4; i < salud.length; i += 4) { %> 
                <div class="carousel-item">
                    <div class="row">
                        <% salud.slice(i, i + 4).forEach((producto) => { %>
                            <div class="col text-center">
                                <div class="product-box">
                                    <img src="<%= producto.imagen %>" class="img-fluid" alt="<%= producto.nombre %>" style="max-height: 200px; object-fit: contain;">
                                    <h5><%= producto.nombre %></h5>
                                    <h3>$<%= producto.precio %></h3>
                                    <!-- AQUI MANDAMOS EL PRODUCTO PARA VER LA INFO METODO POST -->
                                    <form action="" method="post"> 
                                        <input type="hidden" name="productoId" value="<%= producto.id_productos %>">
                                        <button type="submit" class="btn btn-primary mt-2">Seleccionar</button>
                                    </form>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </div>
                <!-- CERRAMOS POR ACA, PARA LLAMAR LOS DATOS SOLO ES CON EL ITERADOR.NOMBREDELATRIBUTO -->
            <% } %> 
        </div>

        <a class="carousel-control-prev" href="#saludCarousel" role="button" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Anterior</span>
        </a>
        <a class="carousel-control-next" href="#saludCarousel" role="button" data-bs-slide="next" style="background-color: #007bff; color: #fff;">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Siguiente</span>
        </a>
    </div>
</div>

<!-- ALIMENTO -->
<div class="container mt-5">
    <h3>ALIMENTO</h3>
    <div id="alimentoCarousel" class="carousel slide" data-bs-ride="false">
        <!-- Indicadores -->
        <ol class="carousel-indicators">
            <% alimento.forEach((producto, index) => { %>
                <li data-bs-target="#alimentoCarousel" data-bs-slide-to="<%= index %>" class="<%= index === 0 ? 'active' : '' %>"></li>
            <% }) %>
        </ol>

        <div class="carousel-inner">
            <div class="carousel-item active">
                <div class="row">
                    <% alimento.slice(0, 4).forEach((producto) => { %>
                        <div class="col text-center">
                            <div class="product-box">
                                <img src="<%= producto.imagen %>" class="img-fluid" alt="<%= producto.nombre %>" style="max-height: 200px; object-fit: contain;">
                                <h5><%= producto.nombre %></h5>
                                <h3>$<%= producto.precio %></h3>
                                <form action="" method="post">
                                    <input type="hidden" name="productoId" value="<%= producto.id_productos %>">
                                    <button type="submit" class="btn btn-primary mt-2">Seleccionar</button>
                                </form>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
            <!-- PARA MOSTRAR LA INFO CORRESPODIENTE UTILIZAMOS UN FOR PARA RECORRER LOS PRODUCTOS CORRESPONDIENTES -->
            <% for (let i = 4; i < alimento.length; i += 4) { %> 
                <div class="carousel-item">
                    <div class="row">
                        <% alimento.slice(i, i + 4).forEach((producto) => { %>
                            <div class="col text-center">
                                <div class="product-box">
                                    <img src="<%= producto.imagen %>" class="img-fluid" alt="<%= producto.nombre %>" style="max-height: 200px; object-fit: contain;">
                                    <h5><%= producto.nombre %></h5>
                                    <h3>$<%= producto.precio %></h3>
                                    <!-- AQUI MANDAMOS EL PRODUCTO PARA VER LA INFO METODO POST -->
                                    <form action="" method="post"> 
                                        <input type="hidden" name="productoId" value="<%= producto.id_productos %>">
                                        <button type="submit" class="btn btn-primary mt-2">Seleccionar</button>
                                    </form>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </div>
                <!-- CERRAMOS POR ACA, PARA LLAMAR LOS DATOS SOLO ES CON EL ITERADOR.NOMBREDELATRIBUTO -->
            <% } %> 
        </div>

        <a class="carousel-control-prev" href="#alimentoCarousel" role="button" data-bs-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Anterior</span>
        </a>
        <a class="carousel-control-next" href="#alimentoCarousel" role="button" data-bs-slide="next" style="background-color: #007bff; color: #fff;">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Siguiente</span>
        </a>
    </div>
</div>



</body>
</html>
